map-config {
  akka {
    log-dead-letters-during-shutdown = off
    log-dead-letters = off
    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
    }
    remote {
      log-remote-lifecycle-events = off
    }
    cluster {
      log-info=off
      roles=["DISTRIBUTED-MAP-NODE"]
      auto-down-unreachable-after = 2s
    }
  }
}